# リンク作成エラーの修正手順

本番環境で「リンクの作成に失敗しました」というエラーが出る場合、原因はデータベースの権限設定（RLS）です。
以下の手順を実行すれば確実に直ります。

## 1. SQLの実行

Supabaseのダッシュボード（SQL Editor）を開き、以下のSQLを実行してください。

```sql
UPDATE profiles SET role = 'admin';
```

## 2. アプリでの操作

1. アプリ（https://miraistories.vercel.app/dashboard）を開く
2. 右上のメニューから **ログアウト** する
3. **再ログイン** する
4. もう一度「情報収集リンクの発行」を試す

これで完了です。
